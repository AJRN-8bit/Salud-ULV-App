// import '../../entities/user.dart';s

abstract class IUserRepository {
  // Future<void> registerUser(IUserRegistry user);
  // Future<void> registerUserInfo(IUserInfoRegistry user);
  // Future<void> loginUser(IUserLogin user);

}